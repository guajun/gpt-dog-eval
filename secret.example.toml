[inference]
# "responses" calls an OpenAI-compatible Responses endpoint. "fake" runs
# deterministically in-process and does not make network requests.
provider = "responses"
base_url = "https://api.openai.com/v1"
api_key = "replace-me"
model = "gpt-6-astra"
reasoning_effort = "medium"
timeout_s = 120.0

